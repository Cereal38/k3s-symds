apiVersion: batch/v1
kind: Job
metadata:
  name: location-setup-user-conf
  namespace: default
spec:
  template:
    spec:
      containers:
        - name: location-setup-user-conf
          image: postgres:16-alpine
          command: ["sh", "-c"]
          args:
            - |
              set -eu
              for f in /scripts/*.sql; do
                echo ">>> applying $f"
                psql -v ON_ERROR_STOP=1 -f "$f"
              done
          imagePullPolicy: Always
          resources:
            requests:
              memory: "128Mi"
              cpu: "500m"
              ephemeral-storage: "1Gi"
            limits:
              memory: "512Mi"
              cpu: "2"
              ephemeral-storage: "5Gi"
          volumeMounts:
            - name: sql
              mountPath: /scripts
              readOnly: true
          env:
            # psql reads all of these implicitly, so the args above need no flags.
            - name: PGHOST
              value: location-pg-rw
            - name: PGPORT
              value: "5432"
            - name: PGDATABASE
              value: location
            # The superuser, not the location app user: the scripts run CREATE ROLE
            # and ALTER DEFAULT PRIVILEGES FOR ROLE location, neither of which the
            # app user is allowed to do. Provided by enableSuperuserAccess on the
            # CNPG cluster (see k8s/db/location-db.yaml).
            - name: PGUSER
              valueFrom:
                secretKeyRef:
                  name: location-pg-superuser
                  key: username
            - name: PGPASSWORD
              valueFrom:
                secretKeyRef:
                  name: location-pg-superuser
                  key: password

      volumes:
        - name: sql
          configMap:
            name: location-sql-script

      restartPolicy: Never
